package in.btm.service;

import java.util.List;

import in.btm.dto.AddressDto;

public interface AddressService {

    AddressDto addAddress(String username, AddressDto dto);

    List<AddressDto> getMyAddresses(String username);

    AddressDto updateAddress(Integer addressId, AddressDto dto, String username);

    void deleteAddress(Long addressId, String username);
}